/**
 * Environment loader
 *
 * Questo modulo esporta l'environment corretto in base alla modalità di build.
 * Angular gestisce automaticamente lo scambio dei file tramite la configurazione di build.
 */

export { environment } from './environment';
